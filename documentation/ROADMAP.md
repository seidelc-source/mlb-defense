# Roadmap

> Living plan — owned by `/ds-plan` (deliberate build/revise) and `/ds-next` (execute and amend). Two rules keep it honest: **detail decays with distance** (P0 concrete with acceptance criteria; P3 one-liners — never plan step 5 at step-1 resolution), and **every item carries kill criteria** ("would change if: <evidence>") so the plan stays adjustable instead of becoming a commitment. Log every change with its reason; move dead items to the graveyard — never silently delete.

## Direction

The descriptive question is answered: the engine's coverage surface, recalibrated per-trajectory, is a valid P(out | standard alignment) that modestly beats zone lookups (VERIFIED). The prescriptive question is half-answered: positioning direction is right after the handedness fix, shifting *broadly* beats always-standard, but the engine's pull-based shift-*selection* adds no value over "shift everyone" and fine x,y placement is untestable without fielder coordinates. This phase ships what is validated (the calibrator), stops shipping what isn't (raw probabilities, the selection heuristic as outcome-validated), and then pursues the two open levers: a shift-selection signal that captures the oracle's +33.5/1000 headroom, and the fielder-coordinate data that would unlock fine-placement validation.

## P0 — now (concrete, acceptance criteria required)

- [x] **DONE 2026-09-02 (commit `08d1c46`)** — Reproducibility floor: initial git commit + persist experiment artifacts out of `/tmp` — **why now**: the repo has ZERO commits and every VERIFIED result (P0 eval, recalibration, P1 Parts 1–2) is backed only by `/tmp/*.json` files and an untracked working tree — one reboot or stray edit and today's evidence is irreproducible; reproducibility sits above validation and production on the priority stack · **done when**: initial commit on `main` covering backend/frontend/documentation; experiment result JSONs + plots copied into a tracked `documentation/artifacts/` (or `backend/artifacts/`) with the EXPERIMENTS.md repro commands pointing at them; `.gitignore` excludes venv/node_modules/.env · **would change if**: — (no evidence makes version control optional).
- [x] **DONE 2026-09-02** — Wire the per-trajectory isotonic calibrator into the alignment service — **outcome**: `scripts/fit_calibrator.py` re-ran the LOSO reliability gate at fit time (**slope 0.992, PASS** — kill criterion did not fire), fit on all 884,402 standard-alignment balls, and persisted `out_calibrator_v1.json` (versioned, tracked, served via `np.interp` with an exact sklearn round-trip). `compute_alignment`/`score_custom_positions` now report `predicted_hit_pct`/`oaa_delta` as calibrated per-ball probabilities aggregated under the batter's *landing* density (not the hit-weighted grid) and blended by the batter's ground/air mix; raw coverage is no longer surfaced as a probability; responses carry `calibrator_version`, persisted rows carry `model_version=0.2.0+cal-v1`, cache keys are calibrator-versioned. 16 new unit tests incl. pinned golden transforms (138 green); smoke passes; served P(out) lands at realistic scale (0.757 vs population 0.693).
- [x] **DONE 2026-09-03** — Stop presenting the pull-based shift recommendation as outcome-validated — **outcome**: `AlignmentResults.tsx` now scopes every displayed number (shift chip labeled heuristic template with tooltip, out/hit % marked "(lg)" league-scale, confidence marked sample-size heuristic, plain-language footnote states OAA Δ is the outcome-validated number); `AlignmentResponse` docstring gained an explicit `shift_type` = heuristic/NOT-outcome-validated paragraph; `suggest_shift_type` docstring cites the P1 Part 2 falsification. 149 backend + 38 frontend tests green, typecheck clean · **would change if**: a new selection signal validates (see P1) — then re-label as validated (decision point A).

## P1 — next (a sentence each)

- [x] **DONE 2026-09-03** — Empirical per-batter landing density — **outcome**: shipped as serving 0.3.0 (`landing.py`, `league_landing_v1.json`, Redis-cached per-batter histograms; σ=2.5, k=200). Pre-registered re-run: level bias +0.0546 → **+0.0052 (PASS)**; batter-signal bar **failed** (pearson −0.10, split-half-robust) → **kill criterion fired as pre-registered**: absolute claims capped permanently at population level, delta remains the decision quantity. Bonus insight: batter out-rate signal lives in EV/trajectory composition, not landing location (→ new P3 idea below).
- [x] **DONE 2026-09-03** — Serving-path bugs: D1 no-spray fallback now uses the league landing prior (was uniform-grid 0.618); D3 `np.roll` weather wrap replaced by zero-fill `_shift2d` (unit-tested no-wraparound).
- [x] **DONE 2026-09-03 (negative — kill criterion fired)** — Find a shift-selection signal with real value — **outcome**: all 3 pre-registered signals (spray dispersion, hard-grounder share, batter sprint speed) FAIL to beat always-shift (best point estimates all negative, CIs span 0, era-half guardrail fails; EXPERIMENTS.md 2026-09-03). **Decision point A fired: "shift broadly" is the policy; per-batter selection → graveyard.** Oracle headroom reinterpreted as mostly noise (max of two noisy arm estimates).
- **Cross-check `compute_reach` against ingested Statcast OAA** (Gap 5 — data already in `fielding_profile`) — why: recalibration fixed the scale, not the physics; a realistic range model is the lever to widen the modest ~0.012 log-loss margin over zone×trajectory · would change if: implied-vs-actual OAA shows the current radii are already unbiased → deprioritize range-model work.
- **Calibrate coordinate transforms against known landmarks** (Gap 3: `hc_x/hc_y`→grid, 1 unit = 400 ft, bases/wall distances) — why: low effort, protects target correctness for everything downstream · would change if: —(cheap enough to just do).

## P2 — later (one-liners)

- Acquire per-play fielder start (x,y) coordinates (Gap 7 — Statcast player-tracking/alignment feeds) to validate fine placement · would change if: no feasible public source after a scoped search → cap the app's claims at categorical/direction-level and park permanently.
- ~~Replace/tune the 8-Gaussian spray zones with empirical `hc_x/hc_y` landing density (Gap 4)~~ — promoted to P1 (2026-09-03) for the *landing-density* half; the hit-mass/positioning half stays here, gated on the P1 result.
- Point-in-time-honest profile lookup for retrospective analyses (as-of-date fielding profile + pre-scenario spray) · would change if: no further backtests are planned.

## P3 — someday / parked

- Batter-level P(out) signal via finer trajectory classes (LD/FB/PU calibrators) and/or exit-velocity-aware calibration — the identified path after landing geometry was ruled out (2026-09-03); pursue only if a batter-specific absolute probability ever becomes a product requirement.
- Arm-strength / double-play modeling in the out conversion.
- Weather-effect validation (carry/drift factors are hand-set, never outcome-checked). *Note 2026-09-02: this is the component the domain expert trusts least (integration interview) — a candidate for promotion to P2 at the next /ds-plan revision, since hand-set factors move displayed P(out) under the confirmed "predicts out conversion" claim bar.*
- FanGraphs xFIP/SIERA pull for `PitcherProfile` (fields reserved, NULL).
- Calibrated uncertainty replacing the `0.30 + n/400` confidence heuristic.

## Decision points

- **A (shift policy)** — **RESOLVED 2026-09-03**: no candidate selection signal beats shift-everyone (4 signals falsified total incl. pull-concentration) → the product recommendation is **"shift broadly": per-batter optimized placement for everyone (legal-constrained), no selection gating**. `suggest_shift_type` stays as a template label only. Reopen only on genuinely new evidence (e.g., a within-PA count-conditional harness or fielder-coordinate data).
- **B (fine placement)**: if fielder-coordinate data (Gap 7) is acquirable → run the non-circular fine-placement eval; the engine's headline x,y feature gets a verdict. If not → the trust ceiling for fine placement stays UNKNOWN and UI claims must stay categorical.
- **C (range model)**: if OAA cross-check (P1) shows reach radii are materially biased → invest in `compute_reach` re-derivation; else accept the modest margin and stop geometry work.

## Change log (newest first)

- 2026-09-03 (decision point A resolved — shift-selection killed) — Pre-registered 3-signal experiment run same day: spray dispersion, hard-grounder share, and batter sprint speed all fail to beat always-shift (best Δs negative, CIs span 0). A fires as pre-registered: policy = "shift broadly" (per-batter optimized placement for everyone), per-batter selection graveyarded, oracle headroom reinterpreted as noise. P1 now has two open items (OAA range cross-check, coordinate calibration).
- 2026-09-03 (published as a static GitHub Pages demo — user decision) — The portfolio demo ships as a free static site (option chosen over paid live hosting): demo mode serves prebaked fixtures for 7 featured matchups × 5 weather presets × 3 optimize targets (516 fixtures generated by `scripts/build_demo_fixtures.py` from the live local engine, calibrator v1, adapter-level tests pin fixture naming). Live-only features (drag re-scoring, arbitrary matchups, ingest, injury editing) are disabled with explanatory copy, so demo claims stay within the validated scope. Repo published to GitHub (public) with Pages CI. Development continues locally against the full stack.
- 2026-09-03 (final P0 closed — shift-suggestion relabel) — UI (`AlignmentResults.tsx`), API schema docstring, and engine docstring now present the shift label as a heuristic template and the absolute percentages as league-scale, with OAA Δ framed as the sole outcome-validated number. Evidence base committed first (`84a684f`). **P0 is now empty**; next work starts at P1 (shift-selection signal → decision point A, OAA range check, coordinate calibration).
- 2026-09-03 (/ds-integrate reconcile) — Refreshed against the current central templates: created `documentation/DATA_MAP.md` (seeded from DATA_KNOWLEDGE.md code-reading facts; edges NOT yet count-validated — `/ds-connect` is the follow-up) and added its entry to the CLAUDE.md Principal DS section. No interview re-run (2026-09-02 interview stands; autonomous session), no P0–P3 changes.
- 2026-09-03 (P1 executed: empirical landing density + serving bugfixes) — Both new P1 items DONE (see P1 ticks). The landing-density kill criterion fired exactly as pre-registered: level repaired (+0.005), batter signal absent (and slightly negative, split-half-robust) → absolute-probability claims capped permanently; all claim surfaces updated (schemas, types, CLAUDE.md). Deviation disclosed in EXPERIMENTS.md (eval slope bar ill-conditioned; roadmap criterion governed). New P3 idea: EV/trajectory-composition calibration. Serving `model_version` 0.3.0; 149 tests green; smoke + live payload verified.
- 2026-09-03 (remediation applied — user decision: option (a) + promote (c)) — Relabel DONE: schemas, engine docstrings, frontend types, and CLAUDE.md no longer claim calibrated absolute probabilities (`oaa_delta` = direction-validated; `hit_pct`/`out_pct` = uncalibrated indexes, ~+0.05 high, HR-excluded estimand). Gap 4's landing-density half promoted P2→P1 as the principled fix; D1/D3 bugfixes added to P1. Tests green post-relabel.
- 2026-09-03 (ds-review falsified the served absolute probabilities) — Red-team + end-to-end reliability check of `calibrated_out_probability`: served P(out) biased +0.055 with no cross-batter signal (EXPERIMENTS.md 2026-09-03, FAIL). The 2026-09-02 P0 tick stands for the wiring *mechanics* (artifact, gate, versioning, tests) but its "served values are calibrated P(hit)" claim is retracted for the absolute fields. **New P0 (before commit/ship): fix or relabel the served aggregation** — decide among (a) relabel absolute fields as unvalidated, (b) population-level anchor, (c) empirical landing density (Gap 4, promotes the P2 item; only option that could restore batter-level signal); also fix the no-spray fallback (D1) and note the np.roll wrap (D3) as a bug. `oaa_delta` direction claims unchanged.
- 2026-09-02 (calibrator shipped) — Ticked P0 item 2: calibrator fit (OOF gate slope 0.992, PASS), persisted as `backend/app/services/alignment/artifacts/out_calibrator_v1.json`, wired into the serving path with version stamping end-to-end; fit report at `documentation/artifacts/calibrator_fit_report.json`. One P0 remains open: stop presenting the pull-based shift suggestion as outcome-validated (the response docstrings now state the scope, but UI copy is untouched).
- 2026-09-02 (/ds-integrate reconcile + domain-expert interview) — Interview on record (user): purpose = **portfolio/demo piece** (win = claims hold up to scrutiny); intended claim = **"predicts out conversion"** (resolves the operating-contract UNKNOWN — full validation bar confirmed, P0 direction unchanged); least-trusted component = **weather carry/drift factors** (noted on the P3 weather item as a P2-promotion candidate); communication preference = plain-first. Also created `documentation/IDEAS.md` and refreshed the CLAUDE.md Principal DS section to the current template. No P0–P3 reordering.
- 2026-09-02 (later, /ds-plan revision) — Added a leading P0 reproducibility item: verified in-code that no persisted calibrator exists (fit-and-discarded inside `scripts/eval_recalibrated.py`; service still returns raw `predicted_hit_pct`, `engine.py:512`), and found that the repo has zero git commits with all experiment artifacts in `/tmp` — reproducibility gates the other P0s on the priority stack. Calibrator-wiring acceptance criteria now note the artifact must be *created* (fit + persisted), not just loaded. No other structural changes; P1–P3, decision points, and graveyard unchanged.
- 2026-09-02 — Initial roadmap at (re-)integration. Reflects state after P0 outcome eval, recalibration PASS, handedness fix, and P1 Parts 1–2: P0 = ship the calibrator + stop over-claiming the shift selection; P1 = selection signal, OAA range check, coordinate calibration; P2 gated on Gap 7 data.

## Graveyard (killed ideas — prevents re-litigating them)

- **Per-batter shift-*selection*** — killed 2026-09-03 (decision point A): four observable signals (pull-concentration, spray dispersion, hard-grounder share, batter sprint speed) all fail to beat "shift everyone" under the within-batter policy-value estimator; the apparent oracle headroom is mostly noise. Policy: shift broadly via per-batter optimized placement. [EXPERIMENTS.md 2026-09-03]
- **Stronger static `infield_shift` template** — killed 2026-09-02: maximally-aggressive-but-legal template vacated the up-the-middle lane; predicted lift went negative. Magnitude belongs to per-batter `optimize_positions`, not a fixed template. [EXPERIMENTS.md]
- **Cross-batter correlation as the lens for judging the optimizer** — killed 2026-09-02: an optimizer tautologically predicts ≥0 benefit for every batter, so per-batter predicted-vs-realized correlation cannot rank it; replaced by the within-batter policy-value estimator. [TRUST_REPORT.md]
- **Pull-concentration as the shift-selection signal** — killed 2026-09-02 (P1 Part 2): V(τ) declines monotonically with selectivity; most-pull batters are not disproportionately the beneficiaries. Selection *value* exists (oracle +33.5/1000) but this signal doesn't capture it. [EXPERIMENTS.md]
